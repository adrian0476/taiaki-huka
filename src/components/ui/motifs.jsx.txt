import React from "react";

export const Koru = ({ className = "", size = 48, strokeWidth = 2 }) => (
  <svg viewBox="0 0 64 64" width={size} height={size} className={className} fill="none" aria-hidden="true">
    <path d="M32 6c14.36 0 26 11.64 26 26S46.36 58 32 58 6 46.36 6 32c0-11.05 8.95-20 20-20s20 8.95 20 20-8.95 20-20 20"
      stroke="currentColor" strokeWidth={strokeWidth} strokeLinecap="round" />
    <path d="M26 32c0-3.31 2.69-6 6-6s6 2.69 6 6-2.69 6-6 6"
      stroke="currentColor" strokeWidth={strokeWidth} strokeLinecap="round" />
  </svg>
);

export const NihoRow = ({ className = "", color = "#E62525" }) => (
  <svg viewBox="0 0 200 12" preserveAspectRatio="none" className={className} aria-hidden="true">
    {Array.from({ length: 20 }).map((_, i) => (
      <polygon key={i} points={`${i * 10},12 ${i * 10 + 5},0 ${i * 10 + 10},12`} fill={color} />
    ))}
  </svg>
);